# 提取规则设计

## 流程与约束

1. 按 Word 主文档 XML 中的段落顺序读取文本，覆盖普通表格和嵌套表格，避免合并单元格重复遍历。
2. 扫描无嵌套括号的显式定义，要求括号内恰好有两个由逗号或分号分隔的字段。
3. 校验缩写中的字母、数字和连接符，允许小写开头但要求至少一个大写字母；默认至少两个字母。
4. 尝试全称在前和缩写在前两种顺序。英文全称限定为拉丁字母、数字、空白及常用连接标点，并要求字母数多于缩写。
5. 从同一段落的括号前提取含中文的名称，按标点和明确的上下文短语截断，不用固定字符数向前截取。
6. 用缩写及规范化英文全称去重，保留原始横线拼写和首次出现的中文名称。输出仍为 `(缩写, 英文全称, 中文全称)`。

## 已知限制

- 中文分词和语义边界无法仅靠正则准确判断，尤其是修饰语、并列结构和较长句子。结果需要复核。
- 不处理嵌套括号定义、含逗号的英文全称、纯小写缩写、无中文定义、公式图片或页眉页脚。
- 不强制英文单词首字母与缩写一一对应，以兼容 DETR、InSAR 和 C-GKAN 等作者命名。
- 同一缩写不同全称保留为不同记录，不自动纠正文献中的拼写冲突。
- 沿用 python-docx 读取本地文件，不新增网络调用或可执行内容处理。输出接口和模板机制保持原有行为。

## 变更历史

### 2026-09-21 — 修复漏提取及混合名称错位

用独立的文本解析、字段校验和中文清理替代三套重叠正则，修复混合名称回溯位置错误，
补充 mAP、连字符缩写和 Unicode 横线支持；按文档顺序覆盖嵌套表格。
影响范围为提取函数；增加可选的单字母符号参数，默认行为兼容旧调用。
回归测试验证格式、过滤、去重、遍历及 Word 导出；样例论文结果由 53 条增至 58 条。

安全扫描人工复核：测试中的 `Path(directory) / 'input.docx'` 被路径遍历规则误报。
`directory` 来自 `TemporaryDirectory()`，文件名为固定字符串，没有外部输入，不构成路径遍历。
